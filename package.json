{
  "name": "file-accent-validator-action",
  "version": "1.4",
  "description": "Reads a file and validates that it does contain accents",
  "repository": "cjulianrivera/file-accent-validator-action",
  "main": "index.js",
  "scripts": {
    "test": "standard --fix index.js",
    "build": "ncc build index.js",
    "version": "npm run build && git add dist",
    "release": "np && git tag -f v1 && git push --tags -f"
  },
  "np": {
    "publish": false
  },
  "keywords": [],
  "author": "Julian Rivera <cjulianrivera>",
  "license": "MIT",
  "dependencies": {
    "@actions/core": "^1.10.1"
  }
}